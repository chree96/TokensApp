import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  priceContainer: {
    marginTop: 20,
    marginBottom: 40,
  },
});

export default styles;
